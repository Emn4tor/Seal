/target
**/target
node_modules
dist
dist-ssr
*.local
.DS_Store

# local dev artifacts — never commit real keys, db files, or app data
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env

# stray uncommitted local experiments, moved out of the real source tree
# rather than deleted outright — see todo.md
/.abandoned-experiments
